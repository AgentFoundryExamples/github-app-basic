# Example environment configuration file
# Copy this to .env and fill in your values

# Application Environment (dev/prod)
APP_ENV=dev

# Server Configuration
PORT=8000
LOG_LEVEL=INFO

# GCP Configuration (optional for dev, required for prod deployments)
# GCP_PROJECT_ID=your-project-id
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json
REGION=us-central

# GitHub App Configuration (required for production)
# Obtain these values from your GitHub App settings page:
# https://github.com/settings/apps/<your-app-name>

# GitHub App ID - numeric identifier for your app
# Example: GITHUB_APP_ID=123456
# GITHUB_APP_ID=

# GitHub App Private Key - PEM formatted private key for JWT signing
# IMPORTANT: For multi-line PEM keys, you have two options:
# 1. Use escaped newlines: "-----BEGIN RSA PRIVATE KEY-----\nMIIE...\n-----END RSA PRIVATE KEY-----"
# 2. Use literal newlines (preserve line breaks when pasting)
# The key MUST include BEGIN and END markers
# Example: GITHUB_APP_PRIVATE_KEY_PEM="-----BEGIN RSA PRIVATE KEY-----\nMIIE...\n-----END RSA PRIVATE KEY-----"
# GITHUB_APP_PRIVATE_KEY_PEM=

# GitHub OAuth Client ID - starts with "Iv1." or "Iv23."
# Example: GITHUB_CLIENT_ID=Iv1.a1b2c3d4e5f6g7h8
# GITHUB_CLIENT_ID=

# GitHub OAuth Client Secret - generated when you create client credentials
# Example: GITHUB_CLIENT_SECRET=1a2b3c4d5e6f7g8h9i0j1k2l3m4n5o6p7q8r9s0
# GITHUB_CLIENT_SECRET=

# GitHub Webhook Secret - secret token for webhook signature verification (optional)
# You can generate this with: openssl rand -hex 32
# If not set, webhook validation will not be available
# Example: GITHUB_WEBHOOK_SECRET=your_webhook_secret_token_here
# GITHUB_WEBHOOK_SECRET=

# GitHub OAuth Redirect URI - callback URL for OAuth flow
# For local development: http://localhost:8000/auth/callback
# For Cloud Run: https://your-service-name.run.app/auth/callback
# Example: GITHUB_OAUTH_REDIRECT_URI=http://localhost:8000/auth/callback
# GITHUB_OAUTH_REDIRECT_URI=

# CORS Configuration (disabled by default)
ENABLE_CORS=false

# Cloud Run Environment Variables
# When deploying to Cloud Run, set these as environment variables in the Cloud Run console
# or via gcloud command:
# gcloud run services update SERVICE_NAME \
#   --set-env-vars GITHUB_APP_ID=123456 \
#   --set-env-vars GITHUB_APP_PRIVATE_KEY_PEM="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----" \
#   --set-env-vars GITHUB_CLIENT_ID=Iv1.abc... \
#   --set-env-vars GITHUB_CLIENT_SECRET=secret... \
#   --set-env-vars GITHUB_WEBHOOK_SECRET=webhook_secret... \
#   --set-env-vars GITHUB_OAUTH_REDIRECT_URI=https://your-service.run.app/auth/callback
